// Placeholder for Phase 7+.
// This target will hold pure-logic modules (PerceptualHash, ActivityRules,
// AnyCodable, JSONL framing helpers) extracted from ProvidenceOverlay so they
// can be unit-tested without AppKit/ScreenCaptureKit.

public enum ProvidenceOverlayCore {
    public static let version = "0.1.0-phase6"
}
